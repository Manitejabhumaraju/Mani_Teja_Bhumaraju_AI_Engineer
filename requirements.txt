# Core
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1

# LLM (NIM is OpenAI-compatible)
openai==1.57.0

# MCP — official Python SDK (requires WSL/Linux for stdio transport)
mcp==1.1.2

# LangGraph + LangChain (compatible range — avoids UV resolver conflicts)
langgraph>=0.2.50
langchain-core>=0.3.15,<0.4.0
langsmith>=0.1.140

# Data + fuzzy match
pandas==2.2.3
rapidfuzz==3.10.1

# Semantic cache (Redis + embeddings)
sentence-transformers==3.3.1
redis==5.2.1
numpy==1.26.4

# Conversation memory
mem0ai>=0.1.28

# Streamlit frontend
streamlit==1.40.2
websockets==13.1
requests==2.32.3

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
fakeredis==2.26.1

# Load testing (optional)
locust==2.32.4
httpx>=0.27.0
